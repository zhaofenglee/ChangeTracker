var abp = abp || {};

abp.modals.changeLogEdit = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("ChangeTracker");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
